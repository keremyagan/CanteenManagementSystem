#ifndef AddProduct_H
#define AddProduct_H
#include <iostream>
using namespace std;
class AddProduct {
	// Private section
	public:
		string name,type;
		double price;
		void add_product(string name,string type,double price);
		// Public Declarations
	protected:
		// Protected Declarations
};

#endif
