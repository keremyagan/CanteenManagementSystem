#ifndef ShowProduct_H
#define ShowProduct_H
#include <iostream>
using namespace std;
class ShowProduct {
	// Private section
	public:
		string name;
		typedef struct Data Struct;
		Struct show_product(string name);
		// Public Declarations
	protected:
		// Protected Declarations
};

#endif
